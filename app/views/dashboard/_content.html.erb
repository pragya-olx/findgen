<div class="bookings_content">
    <div class="col-md-3">
        <div class="list-group">
            <a href="#" class="list-group-item active" id="approved">Active Bookings</a>
            <a href="#" class="list-group-item " id="previous">Previous Bookings</a>
            <a href="#" class="list-group-item " id="pending">Pending Approval</a>
        </div>
        <div>
        <% if current_user.is_admin? %>
            <button type="button" class="btn btn-primary create-booking" data-toggle="modal" data-target="#createBooking">
                Create New Booking
            </button>
        <% end %>
        </div>
    </div>

    <div class="col-md-9">
        <div class="row">
    		<%= render "dashboard/table.html.erb" %>
        </div>
    </div>
</div>